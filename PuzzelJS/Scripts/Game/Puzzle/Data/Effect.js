function Effect(tick, tickEnd, row, col, type, set, combo) {
    this.tick = tick;
    this.tickEnd = tickEnd;
    this.row = row;
    this.col = col;
    this.type = type;
    this.combo = combo;
    this.set = set;
};